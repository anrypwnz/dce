mixin service-sm(picture, title, page)

	a.service-sm(href= page)&attributes(attributes)
		.service-sm__picture
			img.service-sm__image(src="img/" + picture, alt="Picture")
			p.service-sm__title= title
