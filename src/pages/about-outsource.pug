extends ../templates/template.pug

block prepend head
    - var title = "Подробнее"

block content
    +header('header--projects')
    +breadcrumbs('Проекты')
    .container
        .row
            .col-lg-8
                +article('article--project--outsource')
    +footer
